pipeline {
    agent any
    tools {
        // Use the correct Maven tool name configured in Jenkins
        maven 'maven'
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                script {
                    // Use the configured Maven installation
                    bat "mvn clean package"
                }
            }
        }
    }
}
